[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"
  # Bo qua deploy neu commit khong thay doi frontend/
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- frontend/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
